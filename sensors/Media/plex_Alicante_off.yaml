platform: history_stats
name: "Plex Alicante off"
entity_id: media_player.plex_plex_for_samsung_tv_samsung
state: 'off'
type: time
start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
end: '{{ now() }}'
